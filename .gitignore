node_modules/
dist/
.data/
*.log
.env
.env.local
.DS_Store
.scratch/
*.tsbuildinfo
coverage/
deploy/ssh/
# playwright e2e output. The suite's own server, content repo and saved session live in the
# OS temp directory (tablinum-e2e-<port>), so only the report and the traces reach the checkout.
playwright-report/
test-results/
tablinum-e2e-*/
